function [numterms,h,spts,vals] = linearSincfun
% returns fields of a default linear sincfun 
% (domain, sdomain, endvals, scl, weights and mobius 
% will need to be set in the outer function)
numterms = [0 0];
h = 0;
spts = [];
vals = [];
end

% function [numterms,h,spts,vals,domain,sdomain,endvals,scl,weights,mobius] = linearSincfun(domain,endvals)
% % returns fields of a default linear sincfun 
% % (domain, sdomain, endvals, scl, weights and mobius 
% % will need to be set in the outer function)
% numterms = [0 0];
% h = 0;
% spts = [];
% vals = [];
% end

% function varargout = linearSincfun(varargin)
% % returns fields of a default linear sincfun 
% % (domain, sdomain, endvals, scl, weights and mobius 
% % will need to be set in the outer function)
% numterms = [0 0];
% h = 0;
% spts = [];
% vals = [];
% end